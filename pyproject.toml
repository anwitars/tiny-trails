[project]
name = "tiny-trails"
version = "0.1.0"
description = ""
authors = [
    {name = "anwitars"}
]
requires-python = ">=3.12"
dependencies = ["starlette (>=0.47.2,<0.48.0)", "uvicorn (>=0.35.0,<0.36.0)"]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
